// 全局配置
const config = {
  // API基础地址
  apiUrl: 'https://www.wangmian.xyz/api',

  // 超时时间
  timeout: 30000,

  // 开发环境（可以在这里切换）
  env: 'production'  // development 或 production
};


module.exports = config;
